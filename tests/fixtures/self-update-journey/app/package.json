{
  "name": "poise-journey-fixture",
  "private": true,
  "version": "1.0.0",
  "description": "Zero-dependency stand-in for Poise used by the release journey integration test.",
  "type": "module",
  "scripts": {
    "check": "node --test --test-reporter=tap tests/greeting.spec.mjs",
    "build": "node scripts/build.mjs",
    "start": "node dist/server.js"
  }
}
